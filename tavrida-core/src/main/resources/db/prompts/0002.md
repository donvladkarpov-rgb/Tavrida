Таблица ASOP_TERMINALS
REGION_ID убрать.

ДОБАВИТЬ:
ссылку на профиль настроек (для валидатора всегда NULL) (новая таблица), поля (помимо ключевого UID):
                profile_name       varchar
                profile_params     jsonb
    
ссылка на версию ПО (новая таблица), поля (помимо ключевого UID):
                тип терминала (пример Azur)
                версия ПО (пример 2.1.2.bc1a6f5e)
                файл ()
                дата обновления

добавить поля в ASOP_TERMINALS:
модель терминала (AZUR, Feithen итп)
статус (на складе, выдан инженеру, в работе итп)
МОЛа (материально ответственное лицо)
ссылка на TID (для валидатора всегда NULL)
ссылка на ТС. меняется (или нет) каждый раз при начале водительской смены, при завершении смены отчищать не нужно
ссылка на Родителя (это для валидаторов, тк храним всех в одной таблице)


внеси изменения и выдай полный sql для нового варианта базы