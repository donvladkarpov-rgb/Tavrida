# docker compose up
# docker compose down --volumes

services:

  tavrida-postgres:
    container_name: tavrida-postgres
    image: postgres:17
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -q -d $$POSTGRES_DB -U $$POSTGRES_USER"]
      timeout: 45s
      interval: 10s
      retries: 10
    restart: always
    environment:
      TZ: "Europe/Moscow"
      POSTGRES_DB: tavrida
      POSTGRES_USER: tavrida
      POSTGRES_PASSWORD: abirvalg
    ports:
      - 5432:5432
    volumes:
      - ./../docker/db/fill-db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./../docker/db/pgdata/:/var/lib/postgresql/data/
